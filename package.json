{
  "name": "xadrez-dev",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare:engine": "node scripts/copy-stockfish.mjs",
    "dev": "npm run prepare:engine && vite",
    "build": "npm run prepare:engine && tsc -b && vite build",
    "test": "node scripts/test-chess-rules.mjs",
    "check": "npm test && npm run build",
    "preview": "vite preview",
    "postinstall": "npm run prepare:engine"
  },
  "dependencies": {
    "chess.js": "^1.4.0",
    "onnxruntime-web": "^1.30.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "stockfish": "19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^5.0.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.4"
  }
}
